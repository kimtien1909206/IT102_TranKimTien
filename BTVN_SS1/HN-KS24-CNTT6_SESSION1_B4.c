#include <stdio.h>
int main ()
{
    int width = 20;
    int length = 30;
    int perimeter = (width + length) * 2;
    int acreage = width * length;
    printf("%d\n", perimeter);
    printf("%d\n", acreage);
    return 0;
}
